const firebaseConfig={apiKey:"AIzaSyAXqB9_pYZsensijZuFqW7cHMW6mRrob6M",authDomain:"football-auction-arena.firebaseapp.com",databaseURL:"https://football-auction-arena-default-rtdb.asia-southeast1.firebasedatabase.app",projectId:"football-auction-arena",storageBucket:"football-auction-arena.firebasestorage.app",messagingSenderId:"573090943311",appId:"1:573090943311:web:dbc4019a02eb55d9e2f53a",measurementId:"G-BGN6SWJEY6"};firebase.initializeApp(firebaseConfig);const db=firebase.database();
const BUDGET=10000,SQUAD_MAX=15,FORMATIONS={433:[["GK"],["DEF","DEF","DEF","DEF"],["MID","MID","MID"],["FWD","FWD","FWD"]],442:[["GK"],["DEF","DEF","DEF","DEF"],["MID","MID","MID","MID"],["FWD","FWD"]],352:[["GK"],["DEF","DEF","DEF"],["MID","MID","MID","MID","MID"],["FWD","FWD"]],4231:[["GK"],["DEF","DEF","DEF","DEF"],["MID","MID"],["MID","MID","MID"],["FWD"]]};
let roomCode="",myId="p_"+Math.random().toString(36).slice(2,10),myName="",isHost=false,room=null,selectedXIPlayer=null,manualXI={},currentScreen="home";
function $(id){return document.getElementById(id)}function show(id){currentScreen=id;document.querySelectorAll(".screen").forEach(s=>s.classList.remove("active"));$(id).classList.add("active")}function showCreate(){show("createScreen")}function showJoin(){show("joinScreen")}function goHome(){show("home")}function resetHome(){roomCode="";room=null;isHost=false;myName="";show("home")}function money(l){return (Number(l||0)/100).toFixed(2)+" Cr"}function makeCode(){return Math.random().toString(36).slice(2,8).toUpperCase()}function shuffle(a){return[...a].map(v=>[Math.random(),v]).sort((x,y)=>x[0]-y[0]).map(x=>x[1])}function posGroup(p){if(p==="GK")return"GK";if(["CB","LB","RB","DF"].includes(p))return"DEF";if(["CM","CDM","CAM","MF"].includes(p))return"MID";return"FWD"}function squadArr(u){return Object.values(u?.squad||{})}function xiArr(u){return Object.values(u?.manualXI||{})}function addHistory(t){if(roomCode)db.ref(`rooms/${roomCode}/history`).push({text:t,time:Date.now()})}
async function createRoom(){const name=$("hostName").value.trim(),team=$("hostTeam").value.trim()||name+" FC",logo=$("hostLogo").value.trim()||"⚽",pin=$("hostPin").value.trim()||"1234";if(!name)return alert("Enter your name macha");roomCode=makeCode();myName=name;isHost=true;await db.ref("rooms/"+roomCode).set({createdAt:Date.now(),status:"lobby",hostId:myId,hostPin:pin,budget:BUDGET,index:0,currentBid:0,highestBidderId:"",highestBidderName:"",sold:{},unsold:{},history:{},reveal:false,players:shuffle(PLAYERS)});await db.ref(`rooms/${roomCode}/users/${myId}`).set({name:myName,team,logo,purse:BUDGET,squad:{},joinedAt:Date.now()});listenRoom();show("lobby")}
async function joinRoom(){const name=$("joinName").value.trim(),team=$("joinTeam").value.trim()||name+" FC",logo=$("joinLogo").value.trim()||"⚽",code=$("roomCodeInput").value.trim().toUpperCase();if(!name||!code)return alert("Enter name and room code");const snap=await db.ref("rooms/"+code).get();if(!snap.exists())return alert("Room not found");const data=snap.val(),userCount=data.users?Object.keys(data.users).length:0;if(userCount>=8)return alert("Room full. Max 8 teams.");roomCode=code;myName=name;isHost=false;await db.ref(`rooms/${roomCode}/users/${myId}`).set({name:myName,team,logo,purse:data.budget||BUDGET,squad:{},joinedAt:Date.now()});listenRoom();show(data.status==="auction"?"auction":data.status==="formation"?"formation":data.status==="revealed"?"trophy":"lobby")}
function listenRoom(){db.ref("rooms/"+roomCode).on("value",s=>{if(!s.exists())return;room=s.val();isHost=room.hostId===myId;render()})}
function render(){if(!room)return;$("roomCodeText").textContent=roomCode;$("auctionRoomCode").textContent=roomCode;renderLobby();renderAuction();renderFormationStatus();if(room.status==="lobby"&&currentScreen!=="lobby")show("lobby");if(room.status==="auction"&&currentScreen!=="auction")show("auction");if(room.status==="formation"&&currentScreen!=="formation"){show("formation");buildFormationScreen()}if(room.status==="revealed"&&currentScreen!=="trophy"){renderTrophy();show("trophy")}}
function renderLobby(){const users=room.users||{};$("lobbyPlayers").innerHTML=Object.entries(users).map(([id,u])=>`<div class="item"><b>${u.logo||"⚽"} ${u.team||u.name}${id===room.hostId?" 👑":""}</b><span>${money(u.purse)}</span></div>`).join("");$("hostLobbyControls").classList.toggle("hidden",!isHost)}
async function startAuction(){if(!isHost)return;const count=Object.keys(room.users||{}).length;if(count<2&&!confirm("Only one team joined. Start anyway?"))return;const p=room.players[0];await db.ref("rooms/"+roomCode).update({status:"auction",index:0,currentBid:p.base,highestBidderId:"",highestBidderName:""});addHistory("Auction started")}
function renderAuction(){const users=room.users||{},me=users[myId];if(me){$("myTeamName").textContent=(me.logo||"⚽")+" "+(me.team||me.name);$("myPurse").textContent=money(me.purse);$("mySquadCount").textContent=squadArr(me).length+"/"+SQUAD_MAX}const p=room.players?.[room.index];if(p){$("playerRating").textContent=p.rating;$("playerPhoto").textContent=p.emoji||"⚽";$("playerName").textContent=p.name;$("playerMeta").textContent=`${p.country} • ${p.pos} • ${posGroup(p.pos)}`;$("basePrice").textContent=money(p.base);$("currentBid").textContent=money(room.currentBid||p.base);$("highestBidder").textContent=room.highestBidderName?`Highest: ${room.highestBidderName}`:"No bids yet"}$("hostControls").classList.toggle("hidden",!isHost);$("liveBidders").innerHTML=Object.entries(users).map(([id,u])=>`<div class="item"><b>${u.logo||"⚽"} ${u.team||u.name}</b><span>${squadArr(u).length}/15 • ${money(u.purse)}</span></div>`).join("");$("mySquad").innerHTML=me&&squadArr(me).length?squadArr(me).map(x=>`<div class="item"><span>${x.emoji||"⚽"} ${x.name} (${x.pos})</span><b>${money(x.price)}</b></div>`).join(""):`<p class="hint">No players bought yet.</p>`;$("soldTable").innerHTML=Object.values(room.sold||{}).slice(-15).reverse().map(x=>`<div class="item"><span>${x.emoji||"⚽"} ${x.name}</span><b>${x.owner} • ${money(x.price)}</b></div>`).join("")||`<p class="hint">No sold players yet.</p>`;$("historyLog").innerHTML=Object.values(room.history||{}).slice(-20).reverse().map(h=>`<div class="item"><span>${h.text}</span></div>`).join("")||`<p class="hint">History appears here.</p>`}
async function placeBid(addLakhs){if(!room||room.status!=="auction")return alert("Auction not active");const me=room.users?.[myId],p=room.players?.[room.index];if(!me||!p)return;if(squadArr(me).length>=SQUAD_MAX)return alert("Your squad is full");const nextBid=Math.max(room.currentBid||p.base,p.base)+Number(addLakhs);if(nextBid>me.purse)return alert("Not enough purse macha");await db.ref("rooms/"+roomCode).update({currentBid:nextBid,highestBidderId:myId,highestBidderName:me.team||me.name});addHistory(`${me.logo||"⚽"} ${me.team||me.name} bid ${money(nextBid)} for ${p.name}`)}function customBid(){const v=Number($("customBid").value);if(!v)return alert("Enter amount in lakhs");placeBid(v);$("customBid").value=""}
async function sellPlayer(){if(!isHost)return;const p=room.players[room.index],bidder=room.highestBidderId;if(!bidder)return alert("No bidder. Use Unsold.");const u=room.users[bidder],price=room.currentBid||p.base;if(!u||u.purse<price)return alert("Bidder insufficient purse");const obj={...p,price},updates={};updates[`users/${bidder}/purse`]=u.purse-price;updates[`users/${bidder}/squad/${room.index}`]=obj;updates[`sold/${room.index}`]={...obj,owner:u.team||u.name,ownerLogo:u.logo||"⚽"};nextPlayerUpdate(updates);await db.ref("rooms/"+roomCode).update(updates);addHistory(`${p.name} sold to ${u.logo||"⚽"} ${u.team||u.name} for ${money(price)}`)}
async function unsoldPlayer(){if(!isHost)return;const p=room.players[room.index],updates={};updates[`unsold/${room.index}`]=p;nextPlayerUpdate(updates);await db.ref("rooms/"+roomCode).update(updates);addHistory(`${p.name} went unsold`)}
function nextPlayerUpdate(updates){const allFull=Object.values(room.users||{}).length>0&&Object.values(room.users||{}).every(u=>squadArr(u).length>=SQUAD_MAX),next=room.index+1;if(allFull||next>=room.players.length){updates.status="formation";return}const np=room.players[next];updates.index=next;updates.currentBid=np.base;updates.highestBidderId="";updates.highestBidderName=""}
async function endAuction(){if(confirm("End auction and move to formation?")){await db.ref("rooms/"+roomCode).update({status:"formation"});addHistory("Auction ended. Formation stage opened.")}}
function buildFormationScreen(){const me=room?.users?.[myId];if(!me)return;manualXI={};selectedXIPlayer=null;renderFormationPitch();renderFormationSquad()}function changeFormation(){manualXI={};renderFormationPitch();renderFormationSquad()}function renderFormationSquad(){const me=room.users[myId];$("formationSquad").innerHTML=squadArr(me).map((p,i)=>`<div class="item pick" onclick="selectXIPlayer(${i})"><span>${p.emoji||"⚽"} ${p.name} (${p.pos})</span><b>${p.rating}</b></div>`).join("")||`<p class="hint">No squad players.</p>`}
function renderFormationPitch(){const rows=FORMATIONS[$("formationSelect").value]||FORMATIONS["433"];$("formationPitch").innerHTML=rows.map((row,ri)=>`<div class="pitch-row">${row.map((slot,si)=>{const key=ri+"_"+si,p=manualXI[key],wrong=p&&posGroup(p.pos)!==slot;return `<div class="slot ${p?"filled":""} ${wrong?"wrong":""}" onclick="assignSlot('${key}','${slot}')">${p?`${p.emoji||"⚽"}<br>${p.name}<br>${wrong?"-10 penalty":p.rating}`:slot}</div>`}).join("")}</div>`).join("")}
function selectXIPlayer(i){const me=room.users[myId];selectedXIPlayer=squadArr(me)[i];document.querySelectorAll(".pick").forEach(x=>x.classList.remove("selected"));const el=document.querySelectorAll(".pick")[i];if(el)el.classList.add("selected")}function assignSlot(key,slotType){if(!selectedXIPlayer)return alert("Select player first");Object.keys(manualXI).forEach(k=>{if(manualXI[k].name===selectedXIPlayer.name)delete manualXI[k]});manualXI[key]={...selectedXIPlayer,slotType};renderFormationPitch()}
function xiScore(xi){return xi.reduce((sum,p)=>sum+Math.max(0,p.rating-(posGroup(p.pos)!==p.slotType?10:0)),0)}async function saveManualXI(){const xi=Object.values(manualXI);if(xi.length!==11)return alert("Select exactly 11 players macha");const score=xiScore(xi);await db.ref(`rooms/${roomCode}/users/${myId}`).update({manualXI:xi,manualXIScore:score,formationSaved:true});alert("Formation saved 🔥")}
function renderFormationStatus(){if(!room||!room.users||!$("formationStatus"))return;const users=Object.values(room.users);$("formationStatus").innerHTML=users.map(u=>`<div class="item"><b>${u.logo||"⚽"} ${u.team||u.name}</b><span>${u.formationSaved?"✅ Saved":"⏳ Waiting"}</span></div>`).join("");const allSaved=users.length>0&&users.every(u=>u.formationSaved);$("hostRevealControls").classList.toggle("hidden",!(isHost&&allSaved))}
async function revealResults(){if(!isHost)return;await db.ref("rooms/"+roomCode).update({status:"revealed",reveal:true})}function finalScore(u){return(u.manualXIScore||0)+Math.floor((u.purse||0)/1000)+(squadArr(u).length>=11?20:0)}function renderTrophy(){const users=Object.values(room.users||{}).map(u=>({...u,final:finalScore(u)})).sort((a,b)=>b.final-a.final),champ=users[0];$("championCard").innerHTML=champ?`<h2>${champ.logo||"⚽"} ${champ.team||champ.name}</h2><p>Final Score: <b>${champ.final}</b></p><p>Formation Score: ${champ.manualXIScore||0} • Purse Left: ${money(champ.purse)}</p>`:"";$("leaderboard").innerHTML=users.map((u,i)=>`<div class="rank-card"><h3>${i+1}. ${u.logo||"⚽"} ${u.team||u.name} ${i===0?"🏆":""}</h3><p><b>Final:</b> ${u.final} • <b>XI:</b> ${u.manualXIScore||0} • <b>Purse:</b> ${money(u.purse)}</p><p>${xiArr(u).map(p=>`${p.name} (${p.pos}→${p.slotType}${posGroup(p.pos)!==p.slotType?", -10":""})`).join(", ")}</p></div>`).join("")}function copyRoomCode(){navigator.clipboard.writeText(roomCode);alert("Copied: "+roomCode)}
